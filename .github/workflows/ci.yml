name: CI/CD

on:
  push:
    branches: [main, master, baseline]
    tags:
      - "v*"
  pull_request:
    branches: [main, master, baseline]
  schedule:
    # Nightly tests on default branch. No release, no publish.
    - cron: "12 3 * * *"
  workflow_dispatch:
